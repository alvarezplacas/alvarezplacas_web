---
/**
 * OrderList - Lista de pedidos recientes para el dashboard.
 * Propiedad del Agente 2 (Backend/Dashboard).
 */
import { Order } from '../logic/order.js';

interface Props {
    orders: any[];
    title?: string;
}

const { orders, title = "Estado de Procesos" } = Astro.props;
const orderObjects = orders.map(o => new Order(o));
---

<div class="bg-[#1a1a1a] border border-gray-800 rounded-xl overflow-hidden shadow-lg flex flex-col">
    <div class="p-6 border-b border-gray-800 flex justify-between items-center">
        <h3 class="text-lg font-bold text-white flex items-center gap-2">
            <i class="fas fa-tasks text-primary"></i> {title}
        </h3>
    </div>
    <div class="divide-y divide-gray-800">
        {orderObjects.map((order) => (
            <div class="p-4 hover:bg-white/5 transition-colors">
                <h4 class="text-white font-bold text-sm">#{order.id} - {order.cliente}</h4>
                <p class="text-[10px] text-gray-500">{order.status}</p>
            </div>
        ))}
    </div>
</div>
