services:
  web_alvarezplacas:
    build: .
    container_name: alvarezplacas_web
    restart: always
    ports:
      - "4325:4321"
    networks:
      - javiermix_network
    volumes:
      - ./archivos_alvarezplacas:/app/public/images/catalog
    environment:
      - DATABASE_URL=postgresql://alvarez_admin:AlvarezAdmin2026@alvarez_db:5432/alvarezplacas
      - NODE_ENV=production

  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: alvarezplacas_files
    restart: always
    ports:
      - "4326:80"
    volumes:
      - ./archivos_alvarezplacas:/srv
      - ./filebrowser.db:/database.db
    networks:
      - javiermix_network

  alvarez_db:
    image: postgres:15
    container_name: alvarezplacas_db
    restart: always
    environment:
      - POSTGRES_USER=alvarez_admin
      - POSTGRES_PASSWORD=AlvarezAdmin2026
      - POSTGRES_DB=alvarezplacas
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/db-schema.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - javiermix_network

networks:
  javiermix_network:
    external: true

volumes:
  postgres_data:
